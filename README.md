# apache-cassandra-opscenter

This play book installs:

Apache Cassandra Cluster
OpsCenter Agent and Daemon


Usage (Example):
ansible-playbook -vvv -i inventory/cassandra_test apache-cassandra-install.yml -u ansible
